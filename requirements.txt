cat-surf>=1.0.20
nibabel>=3.2
numpy>=1.23.2
# macOS 27 rejects scipy's _spropack.so from the 1.15.x cp310 wheels
# (__thread_bss zero-fill section); cap below 1.14 there. 1.13.1 works.
scipy>=1.10.1; sys_platform != 'darwin' or python_version >= '3.11'
scipy>=1.10.1,<1.14; sys_platform == 'darwin' and python_version < '3.11'
# PyTorch 2.9 dropped cp39 wheels; 2.8 is the last line supporting Python 3.9.
# On macOS the 2.9 MPS kernels are missing under 2.8 -> CPU fallback (slower).
torch>=2.9; python_version >= '3.10'
torch>=2.8,<2.9; python_version < '3.10'
deepmriprep>=0.3.0
deepbet>=1.0.2
torchreg>=0.1.3
spline-resize>=0.1.1
SplineSmooth3D>=0.1.1
nxbc>=0.2.0
requests
flask>=2.3
PySide6>=6.5
vtk>=9.0.0
apscheduler>=3.10
h5py>=3.0
sentry-sdk
